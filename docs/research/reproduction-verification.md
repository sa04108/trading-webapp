# 재현 자료 보존과 전체 결과 대조

2026-09-06. 연구가 `/tmp` 파일의 수명에만 의존하지 않도록 허용한 원자료와 검증 당시
코드를 **원래 사용자 작업공간의 Git 제외 경로**에 복사했다. 기존 입력을 삭제하거나
운영 DB를 변경하지 않았다. 이 보존은 현재 후보의 위험 판정을 바꾸지 않는다.

## 보존 위치와 범위

- 위치: `/home/hyunbae204/work/trading-webapp/data/reliable-strategy-reproduction/2026-09-06-8791951`
- 기준 코드: `8791951a99d9e42750eccf831791cf9f5757aecd`
- 원자료·당시 코드·보고서: **2,619개 파일, 1,652,061,209바이트**
- `research-data/`: 허용된 시장·공시 자료, 공개 ETF·경제 자료, 패널과 실험 기록.
- `snapshot/`: 위 커밋의 연구 코드·보고서와 참조한 편향 검토 문서.
- `MANIFEST.json`: 각 보존 파일의 크기와 SHA-256. 원본과 복사본을 모두 읽어 대조했다.
- `replay/`: 사본에서 재구축한 패널과 재실행 결과.
- `REPLAY-VERIFICATION.json`: 대조 결과와 실행 환경.

보존 디렉터리는 0700, 복사한 파일은 0600 권한이다. 기존 `.gitignore`의 `data/` 규칙으로
원자료는 Git에 포함되지 않는다. API 접속 실행 도우미·개인키·환경변수 파일·로그·가상환경은
복사하지 않았다. 재구축 가능한 `market.sqlite`와 미완료 패널도 제외했다.
이 사본은 같은 로컬 디스크에 있으므로 디스크 장애까지 방어하는 원격 백업은 아니다.

## 실제 재실행으로 확인한 내용

모든 재실행의 코드·입력은 위 사본에서 읽었다. 계산 환경은 기존 Python 3.12.3 및
고정 의존성 버전이며, 새 가상환경 설치까지 검증한 것으로 표시하지 않는다.

| 재현 대상 | 대조 결과 |
|---|---|
| 공개 시장 가격·공식 NAV·분할에서 ETF 패널 재구축 | 14개 파일의 내용이 보존 패널과 정확히 일치 |
| 고정 QLD·GLD 전체 검증 | 일별 평가액·체결·161개 민감도·구간·위기·재표본·감사 값 전부 일치 |
| 두 배당 가설과 31개 오류 시가 경계 | 모든 JSON 값 일치 |
| 국내 고정 후보의 2021~2024 검증 구간 | 모든 JSON 값 일치, 수익률 -17.1831% 유지 |
| 초기 QQQ 추세 후보의 10년 | 모든 JSON 값 일치, 수익 목표 미달 유지 |

전체 분산 검증의 코드 출처 해시는 새 스냅샷 `8791951` 기준으로 바뀌었다. 경제 자료와
운용사 원문 해시는 같고, 출처 메타데이터를 제외한 모든 값이 이전 v3와 정확히 같았다.
국내 결과는 JSON 표기 형식 때문에 파일 해시는 다르지만 파싱한 모든 값·배열은 같다.
실패한 국내 후보의 마지막 2년이나 기각한 방어 가족의 이후 10년을 새로 열지 않았다.

[검증 결과 JSON](reproduction-verification.json)에는 파일 해시와 환경 버전을 담았다.
보존 목록 자체의 SHA-256은
`c4178ba9aaef938f4b5dca92f759bfe9a3f4d7bcdd352f6f67fbe2dfd202764b`다.
이 해시는 기존 원자료·코드 사본 목록에 대한 것이며 이후 생긴 재실행 출력은 별도로 대조했다.

## 목표의 현재 판정

| 조건 | 현재 증거와 판정 |
|---|---|
| 고정 전략 하나와 구현 | QLD 40%·GLD 60% 월간 재조정 구현·규칙 보존 |
| 비용 후 최근 10년 +500% 초과 | 달러 +650.16%로 충족, 분배금 제외·세전 |
| 비중·난수·비용 민감도 검증 | 161개 실행과 실패한 체결 가격 경계까지 보존·재현 |
| 연구에서 정한 달러 초과 샤프 1.0 | 0.9600으로 미달 |
| 연구에서 정한 달러 MDD 35% | -35.1991%로 미달 |
| 여러 시기에서의 신뢰도 | 다른 과거 10년의 지수 열세·위기 NAV 낙폭·사후 선택 편향이 남음 |
| 입력 정합성 | 분배금 전체 원문과 GLD 오류 날짜의 일차 시세는 미확인 |
| 재현 가능한 결과 제공 | 원자료·코드 사본과 위 핵심 결과의 실제 재실행 대조 완료 |

샤프 1.0·MDD 35%는 사용자가 직접 지정한 숫자가 아니라 연구에서 먼저 정한 기준이다.
사후에 낮춰 통과시킨 것으로 표시하지 않는다. 기준 유지 여부를 사용자에게 확인 중이며
답변이 없는 동안 기존 기준을 적용한다. 원문 데이터 확인과 위험 기준 판단도 서로 다른
문제다. 자료가 복원되었다고 위험 관문을 자동 통과시키지 않는다.
