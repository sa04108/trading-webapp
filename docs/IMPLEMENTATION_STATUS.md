# IMPLEMENTATION STATUS

기준: `PLAN.md` / 스펙 §36 Phase 0~5 (MVP)

| Phase | 내용 | 상태 |
|---|---|---|
| 0 | 기반 (스캐폴딩, config, health, 경계 검사) | 완료 |
| 1 | 인증·UI shell (Argon2id, 세션, 감사 로그, 내비게이션) | 완료 |
| 2 | 데이터 (Candle, Parquet/DuckDB, CSV import, coverage, 집계) | 완료 |
| 3 | 엔진 (체결, 비용, 이벤트 루프, 지표, 결정성, look-ahead) | 완료 |
| 4 | 작업 큐 (SQLite 큐, 자식 프로세스, IPC, 취소, 복구) | 완료 |
| 5 | 결과 UI (지표 카드, 차트, 거래 테이블, 위저드, SSE) + E2E | 완료 |
| 6 | AWS 인프라 — 설정 파일 생성 완료 (infra/, scripts/), 실서버 적용은 사용자 작업 | 부분 완료 |
| 7 | 운영 (S3 백업 자동화, 알림, 재해 runbook) | 미착수 |
| 8 | Paper Trading | 미착수 |
| 9 | 정량 전략 2종(횡단면 모멘텀·밸류퀄리티랭킹) + PIT 팩트 저장소(DART 파서·`facts:sync` CLI) + 제출 시 재무 요구 검증(422) | 완료 |
| 10 | 스윙 전략 2종(EMA 추세 스위치·RSI 되돌림) + `hourly-breakout` → `range-breakout` 재작성(추적 손절·비중 상한·O(n²) 제거, D-028) | 완료 |

## MVP 완료 기준 대비 (스펙 §38)

- ✅ 백테스트: 동일 입력 재현(해시 검증), next-bar-open 체결, look-ahead 테스트, 비용 반영, 데이터 버전 저장, 취소, 재시작 INTERRUPTED 복구, 동시 실행 1, 결과·거래 표시
- ✅ 모바일: 390px 가로 스크롤 없음(E2E 검증), 휴대폰 전 과정 가능(위저드), 진행률·취소, 차트 툴팁
- ✅ 보안(앱 계층): 127.0.0.1 bind, 로그인(비밀번호 + TOTP 2단계 — D-017), 세션 회전·만료, 안전한 쿠키, redaction, 임의 코드 실행 없음, 도메인 계층 인프라 비인지(dependency-cruiser 강제)
- ⬜ 보안(인프라): UFW 22/80/443·sshd 키 전용·Caddy TLS 는 서버 구축 시 검증 (infra/provision.sh 준비됨, D-017)
- ⬜ 운영: systemd 자동 시작·backup/restore 검증은 서버 구축 시

## 알려진 제약

- 키움 REST 어댑터는 App Key 발급 전까지 비활성 — CSV/Parquet import 가 데이터 경로 (D-002)
- 토스증권 동기화 파이프라인 구현·와이어링 완료 (D-018·D-020·D-021) — 웹 데이터셋 페이지에서 생성·동기화·심볼 편집·삭제 가능. TOSS_CLIENT_ID/SECRET 설정과 허용 IP 등록만 남음. 1h 봉 미제공(1분봉 집계로 생성), 증분 자동 스케줄러는 미구현(수동 sync 트리거)
- KR 공휴일 캘린더 미반영 — coverage 가 공휴일을 누락으로 표시할 수 있음, UI 에 명시 (D-006)
- 개발 머신 Node 22 / 운영 Node 24 (D-001), TypeScript 5.9 고정 (D-007), better-sqlite3 v12 (D-008)
- 손절·익절은 **종가**로만 판정한다 — 장중에 손절선을 뚫었다가 종가가 회복한 봉은 청산되지 않아 실제 스톱 주문보다 낙관적이다. `execution.fillTiming` 이 `NEXT_BAR_OPEN` 고정이라 장중 체결을 표현할 수 없다. 파라미터 설명에 고지 (`docs/reviews/BACKTEST_BIAS_REVIEW.md` B-008, D-028)
- 과거 `hourly-breakout` 실행은 전략 이름·파라미터 라벨 없이 원본 id/키로 표시되고 「재설정 및 복제」 시 전략을 다시 골라야 한다 — `range-breakout` 으로 대체하며 별칭을 두지 않았다 (D-028)
- 배당·생존 편향 미보정 — 결과 화면 경고에 표시 (§9.4). 액면분할은 횡단면 모멘텀 전략에 한해 분할 이력이 수집된 데이터셋에서 신호 계산 시 보정된다(체결가는 항상 실제 거래 가격) — range-breakout·value-quality-rank 는 자본변동 이벤트를 참조하지 않는다. 거시 지표 수집(`FactScope.MACRO`)·시점별 지수 구성·업종 분류·US 재무제표(SEC EDGAR)는 이번 범위가 아니다. 웹 동기화의 「재무」 체크박스는 **증분**(미수집 연도 + 현재 연도)만 수집한다 — 과거 연도 정정공시를 다시 받으려면 `pnpm cli facts:sync --dataset <id> --from <연도> --to <연도>` 를 쓴다. 웹 수집은 연결(CFS) 고정이며 별도(OFS)는 CLI `--fs-div OFS` 로만 가능하다. **재무 수집은 국내(KR) 종목 전용이다** — DART 는 국내 공시 기관이다

## 검증 게이트 이력

| 일자 | Phase | lint | typecheck | test | build |
|---|---|---|---|---|---|
| 2026-07-25 | 0 | ✅ | ✅ | ✅ 5/5 | ✅ (기동 스모크 통과) |
| 2026-07-25 | 1 | ✅ | ✅ | ✅ 16/16 | ✅ |
| 2026-07-25 | 2 | ✅ | ✅ | ✅ 35/35 | ✅ |
| 2026-07-25 | 3 | ✅ | ✅ | ✅ 58/58 | ✅ |
| 2026-07-25 | 4 | ✅ | ✅ | ✅ 64/64 | ✅ |
| 2026-07-25 | 5 | ✅ | ✅ | ✅ 64/64 + E2E 3/3 | ✅ |
| 2026-07-25 | 리뷰 반영 1차 (C1·I1~I8·M1~M16) | ✅ | ✅ | ✅ 79/79 | ✅ |
| 2026-07-25 | 리뷰 반영 2차 (I9~I14·M17~M25, Codex PR #3) | ✅ | ✅ | ✅ 83/83 | ✅ |
| 2026-07-29 | 9 (정량 전략·PIT 팩트 저장소, Task 1~11) | ✅ | ✅ | ✅ 399/399 | ✅ |
