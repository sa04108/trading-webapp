/**
 * 백테스트 유니버스에 넣을 수 있는 종목 수 상한.
 *
 * 200 — 횡단면 랭킹 전략은 유니버스가 좁으면 '상위 N 선별' 이 의미를 잃는다
 * (설계 2026-07-29-quant-strategies-and-fact-store-design.md). 메모리 상한은 여기가
 * 아니라 `MAX_BACKTEST_BARS` 가 지킨다 (bar-estimate.ts).
 *
 * **요청 스키마에서 떼어내 여기 둔 이유**: 데이터셋은 1,000종목까지 담을 수 있어
 * 화면이 "상한을 넘는 데이터셋" 을 알아보고 상위 N종목만 골라 보내야 한다. 화면이
 * 자기 숫자를 들면 상한을 올릴 때 한쪽만 고쳐지고, 그 어긋남은 제출 422 로만 드러난다.
 * 스키마 파일(zod 의존)을 그대로 import 하면 이 상수 하나 때문에 zod 가 웹 번들에
 * 들어오므로 상수만 따로 담는다.
 */
export const MAX_UNIVERSE_SYMBOLS = 200;
